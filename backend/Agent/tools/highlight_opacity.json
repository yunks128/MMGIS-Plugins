{
  "name": "highlight_opacity",
  "description": "Adjust the opacity of the temporary highlight overlay.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["delta"],
    "properties": { "delta": { "type": "number", "description": "+/- change" } }
  },
  "modelParameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["delta"],
    "properties": { "delta": { "type": "number" } }
  },
  "execution": { "adapter": "custom", "ui": { "type": "highlight_opacity" } }
}

